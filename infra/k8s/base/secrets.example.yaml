# Development template only. Do not apply or commit populated secret manifests.
# Production values must come from the deployment platform's secrets manager.
apiVersion: v1
kind: Secret
metadata:
  name: fieldforge-secrets
type: Opaque
stringData:
  DB_PASSWORD: replace-at-deploy-time
  JWT_SECRET: replace-at-deploy-time
